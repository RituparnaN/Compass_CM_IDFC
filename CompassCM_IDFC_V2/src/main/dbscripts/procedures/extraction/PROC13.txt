CREATE OR REPLACE PROCEDURE PROC13 (
i_vcFromDate IN VARCHAR2, i_vcToDate IN VARCHAR2, i_vcUserCode IN VARCHAR2)
AS
tsStartDateTime TIMESTAMP(6) := SYSTIMESTAMP;
BEGIN
 INSERT INTO TB_DAILYEXTRACTIONIMPORTLOG VALUES('4','PROC13 Started',SYSTIMESTAMP);
 COMMIT;
 
 DBMS_LOCK.Sleep(20);
 
 INSERT INTO TB_DAILYEXTRACTIONIMPORTLOG VALUES('4','PROC13 Completed',SYSTIMESTAMP);
 COMMIT;
 
 INSERT INTO TB_PROCESSLOG(GROUP_ID, SL_NO, PROC_NAME, STARTTIMESTAMP, ENDTIMESTAMP, LOGTIMESTAMP) VALUES (4, 13, 'PROC13', tsStartDateTime, SYSTIMESTAMP, SYSTIMESTAMP);
 COMMIT;
END PROC13;
/
