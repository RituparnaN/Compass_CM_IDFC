CREATE OR REPLACE PROCEDURE PROC12 (
i_vcFromDate IN VARCHAR2, i_vcToDate IN VARCHAR2, i_vcUserCode IN VARCHAR2)
AS
tsStartDateTime TIMESTAMP(6) := SYSTIMESTAMP;
BEGIN
 INSERT INTO TB_DAILYEXTRACTIONIMPORTLOG VALUES('4','PROC12 Started',SYSTIMESTAMP);
 COMMIT;
 
 DBMS_LOCK.Sleep(20);
 
 INSERT INTO TB_DAILYEXTRACTIONIMPORTLOG VALUES('4','PROC12 Completed',SYSTIMESTAMP);
 COMMIT;
 
 INSERT INTO TB_PROCESSLOG(GROUP_ID, SL_NO, PROC_NAME, STARTTIMESTAMP, ENDTIMESTAMP, LOGTIMESTAMP) VALUES (4, 12, 'PROC12', tsStartDateTime, SYSTIMESTAMP, SYSTIMESTAMP);
 COMMIT;
END PROC12;
/
